{
  "rules": {
    ".read": "true",
    ".write": "true",
    "contacts": {
      "$contact": {
        "name": {
          ".validate": "newData.val() != null && newData.isString() && newData.val().length < 49 && newData.val().length > 1"
        },
        "bnumber": {
          ".validate": "newData.val() != null && newData.val().length == 9"
        },

        "pbusiness": {
          ".validate": "newData.val() != null && newData.isString() && newData.val().matches(/Fisher|Distributor|Processor|Fish Monger/)"
        },
        "address": {
          ".validate": "newData.isString() && newData.val().length < 50"
        },
        "provence": {
          ".validate": "newData.val() == null || (newData.isString() && (newData.val().matches(/AB|BC|MB|NB|NL|NS|NT|NU|ON|PE|QC|SK|YT/)))"
        }
      }
    }
  }
}